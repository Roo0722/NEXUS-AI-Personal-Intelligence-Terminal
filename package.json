{
  "name": "nexus-ai-terminal",
  "version": "2.0.0",
  "description": "NEXUS AI Personal Intelligence Terminal - Deployed on Cloudflare Pages",
  "main": "index.html",
  "scripts": {
    "dev": "wrangler pages dev . --local",
    "deploy": "wrangler pages deploy . --project-name=nexus-ai-terminal",
    "build": "echo 'No build step required for static site'"
  },
  "dependencies": {},
  "devDependencies": {
    "wrangler": "^3.63.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
