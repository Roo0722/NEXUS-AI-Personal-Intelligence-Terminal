name = "nexus-ai-terminal"
compatibility_date = "2024-01-01"
pages_build_output_dir = "."

# Environment variables should be set in the Cloudflare Dashboard 
# or passed via --env flag during local dev.
# Do not commit actual keys to this file if pushing to public GitHub.
